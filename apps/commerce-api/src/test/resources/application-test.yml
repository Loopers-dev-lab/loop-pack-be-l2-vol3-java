spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

queue:
  enabled: true
  max-size: 100
  entry-token:
    ttl-seconds: 10
  scheduler:
    enabled: false               # 테스트에서 스케줄러 자동 실행 방지
    interval-ms: 100
    batch-size: 5
