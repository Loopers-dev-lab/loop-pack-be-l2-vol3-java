P0(Critical)

- 메시지 규격(스키마)이 바뀌어야 하는 경우도 있을것 같은데, 이런 경우 스키마가 변경된 토픽에 대해서 컨슈머 그룹측에서는 처리를 어떻게 하나요? 역직렬화가 실패한다거나, 컨슈머에서 재배포를 해도 변경 전의 스키마를 읽어서 역시 역직렬화 실패가 일어날 수도 있을것 같은데 처리방식이 궁금합니다. (윤유탁)

    ```jsx
    토픽도 보통 버저닝을 함.
    
    XX-YY-KK.0
    XX-YY-KK.1 << 토픽 명 마지막에 버전을 붙임.
    ```

- ApplicationEvent로 충분한 것과 Kafka로 가야 하는 것의 경계가 뭔가요? 과제에서도 Step 1에서 ApplicationEvent로 분리한 뒤 Step 2에서 "시스템 간 전파가 필요한 것"만 Kafka로 올리라고 하는데, 이 "필요한 것"을 판단하는 실무적인 기준이 궁금합니다.(임동욱)

    ```jsx
    실무적인 기준에서 "애플리케이션 이벤트" 왜 쓰냐.
    
    코드의 복잡도가 너무 높아짐.
    애플리케이션 이벤트를 쓰는 이유 = 도메인 결합도를 낮추기 위함.
    그런데, 결제 파사드가 결제 이메일 발송해주는걸 몰라야할까? 알아도 되는거 아닌가?
    코드를 추적하는 비용이 너무 높아짐.
    
    @TransactionalEventListener << 트랜잭션에 대한 Hook 과 같은 기능.
    커밋이전, 커밋이후,...
    "트랜잭셔널 아웃박스" 이벤트 같이..
    
    발송이 안되면? 그 작업 자체도 무효화되는 게 맞음.
    발송함의 메일을 "같은 트랜잭션 안" 에서 저장하는게 맞는 이념...
    
    아웃박스는 여기저기서 아는앤데. 얘도 알고 쟤도 알아야해?
    하나의 훅으로 만들어 놓으면... 오히려 편할 수 있다.
    
    + @Async 와 같이 쓰는 "비동기 작업" + "관심사 분리" 
    ```

- 멘토님은 처음 토픽을 생성할 때 파티션 개수를 산정하는 멘토님만의 기준이 있으신가요? 예상량을 기반으로 미리 넉넉하게 잡는 편이신지 아니면 점진적으로 늘려가는 방식을 선호하시는지 궁금합니다. (한정현)

    ```jsx
    3, 6, 9
    브로커 * n ..
    
    최소로 최대한.
    모니터링 통해서 늘려가는 편. (kafka lag = 처리량이 딸리는지)
    ```


P1 (Important)

- 모놀리식 프로젝트에서 이벤트 기반 처리를 도입하는 게 실제로 이점이 있을까요? 서비스 레이어에서 트랜잭션을 직접 나눠서 호출하는 방식 대비, 굳이 이벤트로 분리하는 게 복잡도만 높이는 건 아닌지 판단이 잘 안 됩니다.(임동욱)

    ```jsx
    스프링 애플리케이션 이벤트? << 굳이? 정말 필요한게 아니라면..
    
    트랜잭션의 경계에 맞게, 자동발동되는 것처럼 묶어준다던지 하는 목적이 아니라면
    현업에서는 굳이 쓰지 않는게..
    
    모놀리틱 + 카프카 ? << 자기가 PRODUCER 이자 CONSUMER ?
    이거는 좀 쓸만한 케이스들이 있음.
    
    엑셀 레코드 1개당 10초 걸리는 작업을 해야함. 
    엑셀 10만개짜리 업로드하면?
    
    100만초 기다리면 되겠지..?
    ```

- 카프카 운영시 유심히 측정해야할 지표가 어떤것이 있나요? ( 윤유탁)

    ```jsx
    Message Lag << 쌓이면 쌓일 수록 처리량이 떨어진다는 소리고. 그만큼 이벤트가
    처리가 되어야할 게 안되고 있단 소리.
    
    브로커 서버들의 CPU, I/O 
    ```


P2 (Curios/Optional)

- 카프카 사용의 이점 중 하나가 대용량 데이터 처리라고 생각되는데, outbox 패턴이나 멱등성 보장 작업을 RDB를 통하면 RDB가 그 처리량을 따라갈 수 있을지 궁금합니다… Redis가 정답일까요?? ( 윤유탁)

    ```jsx
    APPEND-ONLY 이기 때문에. 아웃박스는 "내가 발송했다" 라는 기록이니까.
    
    멱등성 보장에서는 좀 다를 수 있음.
    
    INBOX 테이블 (OUTBOX 반대 = 내가 수신했다) 라는 기록을 남기고 
    "처리중이다" 라는 상태를 마킹해놓으면
    동일한 Key 의 메세지가 들어왓을 때 "너 기다려"
    ```

- PG 승인은 됐는데 카프카 커밋이 실패하는 경우가 처리가 까다롭다고 생각되는데, 실제로는 어떻게 처리하시는지 궁금합니다. (윤유탁)

    ```jsx
    PG 승인 (외부 API) = 성공
    카프카 퍼블리시 -> 실패
    
    결제는 되었는데, downstream 은 모르고 잇는 상황 (이벤트 유실)
    
    OutboxPattern : DB 에 이벤트를 같이 저장한다 컨셉.
    Outbox 에 저장하고 바로 쏴도 되지만..
    
    Outbox 가 지키고자 하는 거는 이벤트 발행의 보장이기 때문에
    그냥 아웃박스에 쌓기만 하고
    카프카 publish 는 주기적으로 배치가  해도 됨.
    
    PG 처리 -> TX { 결제 상태 저장 / outbox insert } 
    									CDC (ChangedDataCapture) 
    									
    									
    
    ~보상 트랜잭션 : PG 취소 API 날리면 됨.~ (x)
    
    (1) OutboxPattern 으로 최대한 발행을 보장한다.
    		=> 결제 상태가 저장안된 케이스를 추적할 수 있기 때문.
    (2) Kafka 발행 실패하면, 비동기 스케줄러로 계속 재시도 (최대한 보내보기)
    (3) 컨슈머 쪽에서 최대한 멱등하게 받아줄 처리를 항상 해야한다.
    		왜? kafka 의 특성상 produce 가 안되면 망하기 때문에 (메세지 유실)
    				producer 는 어떻게든 발행하려고 중복 발행이고 나발이고 계속 쏘는게 차라리 나음.
    ```