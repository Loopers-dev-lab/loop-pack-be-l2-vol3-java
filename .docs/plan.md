# Plan

## Authentication (인증)

### User Authentication (회원 인증)

인증 헤더: `X-Loopers-LoginId`, `X-Loopers-LoginPw`

- [x] 인증이 필요한 API는 인증 헤더가 없으면 `401 Unauthorized`를 반환한다
- [x] 인증 헤더의 로그인 ID가 존재하지 않으면 `401 Unauthorized`를 반환한다
- [x] 인증 헤더의 비밀번호가 일치하지 않으면 `401 Unauthorized`를 반환한다
- [x] 인증된 사용자 정보를 컨트롤러에서 사용할 수 있다

---

### Admin Authentication (어드민 인증)

인증 헤더: `X-Loopers-Ldap`

- [x] `X-Loopers-Ldap` 헤더의 값이 `loopers.admin`과 일치하는지 확인한다
- [x] 헤더가 없거나 값이 일치하지 않으면 `401 Unauthorized`를 반환한다
- [x] `/api-admin/v1/**` 경로에 어드민 인증을 적용한다

---

## User (회원)

### 회원가입

입력 정보: 로그인 ID, 비밀번호, 이름, 생년월일, 이메일

- [x] 로그인 ID는 영문과 숫자만 허용한다
- [x] 이메일은 `xx@yy.zz` 형식이어야 한다
- [x] 생년월일은 `yyyy-MM-dd` 형식이어야 한다
- [x] 이름은 빈 값일 수 없다
- [x] 비밀번호는 8~16자의 영문 대소문자, 숫자, 특수문자만 허용한다
- [x] 비밀번호에 생년월일을 포함할 수 없다
- [x] 비밀번호는 암호화하여 저장한다
- [x] 이미 가입된 로그인 ID로는 가입할 수 없다

---

### 내 정보 조회

반환 정보: 로그인 ID, 이름, 생년월일, 이메일

- [x] 이름의 마지막 글자는 `*`로 마스킹한다
- [x] 존재하지 않는 로그인 ID로 조회하면 `404 Not Found`를 반환한다

---

### 비밀번호 수정

입력 정보: 기존 비밀번호, 새 비밀번호

- [x] 기존 비밀번호가 일치해야 수정할 수 있다
- [x] 새 비밀번호는 비밀번호 규칙을 따라야 한다
- [x] 현재 비밀번호와 동일한 비밀번호로는 수정할 수 없다

---

## Brand (브랜드)

### [어드민] 브랜드 등록

`POST /api-admin/v1/brands` · 입력: 브랜드 이름(필수), 로고 URL(필수), 설명(선택)

- [x] 브랜드 이름, 로고 URL, 설명을 입력받아 브랜드를 생성한다
- [x] 브랜드 이름은 2자 이상 50자 이하여야 한다
- [x] 활성 상태의 브랜드 중 동일한 이름이 있으면 등록에 실패한다
- [x] 삭제된 브랜드와 동일한 이름으로 재등록이 가능하다

---

### [어드민] 브랜드 목록 조회

`GET /api-admin/v1/brands`

- [x] 등록된 브랜드를 페이지 단위로 조회한다
- [x] 삭제된 브랜드도 조회 대상에 포함한다
- [x] 기본 정렬은 생성일 내림차순이다

---

### [어드민] 브랜드 상세 조회

`GET /api-admin/v1/brands/{brandId}`

- [x] 단일 브랜드의 전체 정보를 조회한다 (삭제된 브랜드 포함)
- [x] 존재하지 않는 브랜드는 조회할 수 없다

---

### [어드민] 브랜드 정보 수정

`PUT /api-admin/v1/brands/{brandId}` · 입력: 이름(필수), 로고 URL(필수), 설명(선택)

- [x] 브랜드 이름, 로고 URL, 설명을 입력받아 브랜드 정보를 수정한다
- [x] 브랜드 이름은 2자 이상 50자 이하여야 한다
- [x] 자기 자신을 제외한 활성 상태의 브랜드 중 동일한 이름이 있으면 수정에 실패한다
- [x] 존재하지 않는 브랜드는 수정할 수 없다
- [x] 삭제된 브랜드는 수정할 수 없다

---

### [어드민] 브랜드 삭제

`DELETE /api-admin/v1/brands/{brandId}`

- [x] 브랜드를 삭제한다
- [x] 존재하지 않는 브랜드는 삭제할 수 없다
- [x] 이미 삭제된 브랜드를 삭제하면 아무 동작 없이 성공한다 (멱등성)
- [x] 해당 브랜드에 소속된 상품의 좋아요를 모두 삭제한다
- [x] 해당 브랜드에 소속된 상품을 모두 삭제한다
- [x] 단일 트랜잭션으로 처리하며, 중간 실패 시 전체 롤백된다

---

### [대고객] 브랜드 상세 조회

`GET /api/v1/brands/{brandId}` · 인증 불필요

- [x] 단일 브랜드 정보를 조회한다
- [x] 존재하지 않거나 삭제된 브랜드는 조회할 수 없다

---

## Product (상품)

### [어드민] 상품 등록

`POST /api-admin/v1/products` · 입력: brandId, 상품명(필수), 가격(필수), 재고(필수), 썸네일 URL(필수), 상세 설명(선택)

- [x] 상품 정보를 입력받아 상품을 생성한다
- [x] 상품명은 2자 이상 100자 이하여야 한다
- [x] 가격은 0 이상이어야 한다
- [x] 재고는 1 이상이어야 한다
- [x] brandId에 해당하는 브랜드가 존재하지 않거나 삭제된 상태이면 실패한다

---

### [어드민] 상품 목록 조회

`GET /api-admin/v1/products` · 선택 파라미터: brandId

- [x] 등록된 상품을 페이지 단위로 조회한다 (기본 20건, 삭제된 상품 포함)
- [x] brandId가 전달된 경우 해당 브랜드의 상품만 필터링한다
- [x] brandId가 전달되지 않으면 전체 상품을 조회한다
- [x] 존재하지 않는 브랜드로는 필터링할 수 없다
- [x] 기본 정렬은 생성일 내림차순이다

---

### [어드민] 상품 상세 조회

`GET /api-admin/v1/products/{productId}`

- [x] 단일 상품의 정보를 조회한다 (삭제된 상품 포함)
- [x] 존재하지 않는 상품은 조회할 수 없다

---

### [어드민] 상품 정보 수정

`PUT /api-admin/v1/products/{productId}` · 입력: 이름(필수), 가격(필수), 재고(필수), 썸네일 URL(필수), 상세 설명(선택)

- [x] 상품 정보를 입력받아 수정한다
- [x] 상품명은 2자 이상 100자 이하여야 한다
- [x] 가격은 0 이상이어야 한다
- [x] 재고는 1 이상이어야 한다
- [x] 존재하지 않는 상품은 수정할 수 없다
- [x] 삭제된 상품은 수정할 수 없다
- [x] 소속 브랜드는 수정할 수 없다

---

### [어드민] 상품 삭제

`DELETE /api-admin/v1/products/{productId}`

- [x] 상품을 삭제한다
- [x] 존재하지 않는 상품은 삭제할 수 없다
- [x] 이미 삭제된 상품을 삭제하면 아무 동작 없이 성공한다 (멱등성)
- [x] 해당 상품에 등록된 좋아요를 모두 삭제한다
- [x] 단일 트랜잭션으로 처리하며, 중간 실패 시 전체 롤백된다

---

### [대고객] 상품 목록 조회

`GET /api/v1/products` · 인증 불필요 · 선택 파라미터: brandId, sort

- [x] 활성 상태의 상품을 페이지 단위로 조회한다
- [x] 삭제된 상품과 삭제된 브랜드의 상품은 조회 대상에서 제외한다
- [x] brandId가 전달된 경우 해당 브랜드의 상품만 필터링한다
- [x] brandId를 생략하면 전체 활성 브랜드의 상품을 반환한다
- [x] 존재하지 않거나 삭제된 브랜드로는 필터링할 수 없다
- [x] 정렬 옵션을 지원한다 (등록일 내림차순, 가격 오름차순, 좋아요 수 내림차순)
- [x] 기본 정렬은 등록일 내림차순이다
- [x] 유효하지 않은 정렬 옵션을 전달하면 요청에 실패한다

---

### [대고객] 상품 상세 조회

`GET /api/v1/products/{productId}` · 인증 불필요

- [x] 단일 상품의 상세 정보를 조회한다 (좋아요 수 포함)
- [x] 존재하지 않거나 삭제된 상품은 조회할 수 없다

---

## Like (좋아요)

### [대고객] 상품 좋아요 등록

`POST /api/v1/products/{productId}/likes` · 인증 필요

- [x] 좋아요를 등록한다
- [x] 이미 좋아요가 존재하면 아무 동작 없이 성공한다 (멱등성)
- [x] 존재하지 않거나 삭제된 상품에는 좋아요를 등록할 수 없다
- [x] 한 사용자는 동일한 상품에 하나의 좋아요만 등록할 수 있다

---

### [대고객] 상품 좋아요 취소

`DELETE /api/v1/products/{productId}/likes` · 인증 필요

- [x] 좋아요를 취소한다
- [x] 좋아요가 존재하지 않으면 아무 동작 없이 성공한다 (멱등성)
- [x] 존재하지 않거나 삭제된 상품의 좋아요는 취소할 수 없다

---

### [대고객] 내가 좋아요한 상품 목록 조회

`GET /api/v1/users/me/likes` · 인증 필요

- [x] 인증된 유저가 좋아요한 상품 목록을 페이지 단위로 조회한다
- [x] 삭제된 상품과 삭제된 브랜드에 소속된 상품은 조회 대상에서 제외한다

---

## Order (주문)

### [대고객] 주문 요청

`POST /api/v1/orders` · 인증 필요 · 입력: 상품 ID와 수량 목록

- [x] 상품 ID와 수량 목록을 입력받아 주문을 생성한다
- [x] 주문 항목이 1건이면 상품명, 2건 이상이면 "첫 상품명 외 N건"으로 주문 이름을 생성한다
- [x] 주문 항목은 1개 이상이어야 한다
- [x] 각 주문 항목의 수량은 1 이상이어야 한다
- [x] 같은 상품이 중복 포함되면 요청에 실패한다
- [x] 존재하지 않거나 삭제된 상품이 포함되면 주문에 실패한다
- [x] 매진(재고 0)인 상품이 포함되면 실패한다
- [x] 재고가 요청 수량보다 적은 상품이 포함되면 실패한다
- [x] 여러 상품 중 하나라도 재고가 부족하면 주문 전체가 실패한다
- [x] 주문 시점의 상품 정보(이름, 가격, 썸네일)를 주문 항목에 스냅샷으로 저장한다
- [x] 주문 항목별 소계(가격 × 수량)와 주문 총액을 계산한다
- [x] 주문 생성 시 상태는 CREATED로 설정한다
- [x] 재고 차감과 주문 생성은 단일 트랜잭션으로 처리하며, 중간 실패 시 전체 롤백된다

---

### [대고객] 주문 목록 조회

`GET /api/v1/orders` · 인증 필요 · 필수 파라미터: startDate, endDate

- [x] 인증된 사용자 본인의 주문 목록을 페이지 단위로 조회한다
- [x] 주문일 기준으로 시작/종료 시간으로 필터링한다
- [x] startDate와 endDate는 필수 파라미터이다
- [x] 기본 정렬은 주문일 내림차순이다
- [x] 주문이 없으면 빈 페이지를 반환한다

---

### [대고객] 주문 상세 조회

`GET /api/v1/orders/{orderId}` · 인증 필요

- [x] 주문 상세 정보를 조회한다
- [x] 존재하지 않는 주문은 조회할 수 없다
- [x] 다른 사용자의 주문을 조회하면 실패한다

---

### [어드민] 주문 목록 조회

`GET /api-admin/v1/orders`

- [x] 전체 주문을 페이지 단위로 조회한다
- [x] 모든 주문 상태의 주문을 조회 대상에 포함한다
- [x] 기본 정렬은 생성일 내림차순이다
- [x] 주문이 없으면 빈 페이지를 반환한다

---

### [어드민] 주문 상세 조회

`GET /api-admin/v1/orders/{orderId}`

- [x] 주문 상세 정보를 조회한다
- [x] 존재하지 않는 주문은 조회할 수 없다
- [x] 소유권 검증 없이 모든 주문을 조회할 수 있다
- [x] 주문자 이름은 마스킹하여 반환한다

---

## Coupon (쿠폰)

### [어드민] 쿠폰 등록

`POST /api-admin/v1/coupons` · 입력: 쿠폰명(필수), 쿠폰 유형(필수), 할인 값(필수), 최대 할인 금액(정률 시 필수), 최소 주문 금액(필수), 만료일(필수)

- [x] 쿠폰 정보를 입력받아 쿠폰을 생성한다
- [x] 쿠폰 유형은 정액(FIXED) 또는 정률(RATE)이다
- [x] 쿠폰명은 2자 이상 50자 이하여야 한다
- [x] 할인 값은 1 이상이어야 한다
- [x] 정률 쿠폰의 할인 비율은 1 이상 100 이하여야 한다
- [x] 정률 쿠폰은 최대 할인 금액이 필수이다
- [x] 최소 주문 금액은 필수이다
- [x] 만료일은 현재 시점 이후여야 한다

---

### [어드민] 쿠폰 목록 조회

`GET /api-admin/v1/coupons`

- [x] 등록된 쿠폰을 페이지 단위로 조회한다
- [x] 삭제된 쿠폰도 조회 대상에 포함한다
- [x] 기본 정렬은 생성일 내림차순이다

---

### [어드민] 쿠폰 상세 조회

`GET /api-admin/v1/coupons/{couponId}`

- [x] 단일 쿠폰의 전체 정보를 조회한다 (삭제된 쿠폰 포함)
- [x] 존재하지 않는 쿠폰은 조회할 수 없다

---

### [어드민] 쿠폰 수정

`PUT /api-admin/v1/coupons/{couponId}` · 입력: 쿠폰명(필수), 할인 값(필수), 최대 할인 금액(RATE 시 필수), 최소 주문 금액(필수), 만료일(필수)

- [x] 쿠폰명, 할인 값, 최대 할인 금액, 최소 주문 금액, 만료일을 입력받아 쿠폰 정보를 수정한다
- [x] 쿠폰 유형은 수정할 수 없다
- [x] 존재하지 않는 쿠폰은 수정할 수 없다
- [x] 삭제된 쿠폰도 수정할 수 있다

---

### [어드민] 쿠폰 삭제

`DELETE /api-admin/v1/coupons/{couponId}`

- [x] 쿠폰을 삭제한다 (soft delete)
- [x] 존재하지 않는 쿠폰은 삭제할 수 없다
- [x] 이미 삭제된 쿠폰을 삭제하면 아무 동작 없이 성공한다 (멱등성)
- [x] 삭제 후에도 이미 발급된 보유 쿠폰은 유지된다

---

### [대고객] 쿠폰 발급

`POST /api/v1/coupons/{couponId}/issue` · 인증 필요

- [x] 쿠폰 ID를 입력받아 해당 쿠폰을 사용자에게 발급한다
- [x] 존재하지 않거나 삭제된 쿠폰은 발급할 수 없다
- [x] 만료된 쿠폰은 발급할 수 없다
- [x] 동일한 쿠폰을 중복 발급할 수 없다 (1인 1매 제한)

---

### [대고객] 내 쿠폰 목록 조회

`GET /api/v1/owned-coupons` · 인증 필요

- [ ] 인증된 유저가 보유한 쿠폰 목록을 페이지 단위로 조회한다
- [ ] 각 보유 쿠폰의 상태(AVAILABLE, USED, EXPIRED)를 반환한다
- [ ] EXPIRED는 쿠폰의 만료일 기준으로 실시간 판정한다 (DB 상태가 AVAILABLE이어도 만료일이 지났으면 EXPIRED)
- [ ] 보유 쿠폰이 없으면 빈 페이지를 반환한다
