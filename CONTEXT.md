# 📚 Weekly Study Context

> 이 파일은 집/회사 어디서든 같은 컨텍스트를 유지하기 위한 파일입니다.
> 세션 시작 시 "컨텍스트 불러와줘" 라고 말하면 Claude가 이 파일을 읽고 바로 이어집니다.

---

## 🎓 학습 진행 방식

### 순서
```
1단계: 개념 잡기        → 모르면 Claude한테 물어보기, Mermaid로 정리
2단계: 개념 퀴즈        → 중간중간 Claude가 질문 → 내가 답변 → 확인
3단계: 시뮬레이션       → Phase 1/2 실행 (문제 직접 겪기)
4단계: 설계             → 고민 목록 → 결정 사항으로 이동
5단계: 코드 구현        → TDD (Red → Green → Refactor)
6단계: Phase 3 검증     → Before/After 수치 비교
7단계: PR + 블로그 + WIL
```

### 개념 학습 규칙
- 개념 정리 요청 시 → Claude가 Mermaid 다이어그램으로 `docs/concepts/` 에 저장
- 각 개념 끝나면 → Claude가 퀴즈 1~2개 출제, 내가 답변 후 확인
- 퀴즈 통과 기준 → 아래 개념 체크리스트에 ✅ 표시

### 📚 토요일 수업 핵심 요약 (사전 학습 완료)
> 이미 배운 개념들. 깊이가 필요한 것만 아래 체크리스트에서 추가 학습.

**Back-pressure** — 시스템이 처리 가능한 속도를 초과하는 요청을 의도적으로 지연/제한. 대기열의 본질.

**전체 흐름**
```
사용자 요청 → Redis 대기열 등록 → 순번 계산
  → 스케줄러: 상위 N명 토큰 발급 → 토큰 보유자만 주문 API 접근
  → 토큰 사용/만료 → Event Listener → Redis 토큰 제거
```

**흔한 실수**
- 대기열 없이 바로 DB 접근 → 트래픽 증가 시 즉시 장애
- Polling 주기 너무 짧게 → 오히려 서버 부하 증가
- 토큰 TTL 미설정 → 유령 사용자 증가
- 순번 계산을 DB에서 수행 → 성능 저하

---

### 📖 개념 체크리스트
> ✅ = 퀴즈 통과 / 🔵 = 수업에서 개요 학습 / [ ] = 미학습

#### Redis 자료구조
- ✅ **Sorted Set 기본** — ZADD, ZRANK, ZREM, ZCARD 동작 원리
- ✅ **ZADD NX 옵션** — 없을 때만 등록, score 덮어쓰기 방지
- 🔵 **score 설계** — timestamp 충돌 시 undefined (해결 전략은 설계 시 결정)
- ✅ **TOCTOU 문제** — 확인+등록 사이에 끼어드는 race condition → ZADD NX로 원자적 처리

#### 대기열 시스템 설계
- ✅ **Back-pressure** — 처리 가능한 만큼만 받는 것. 대기열의 본질
- ✅ **대기열 vs Rate Limiting** — Rate Limiting은 거절 후 재시도 반복(카오스), 대기열은 번호 받으면 경쟁 끝
- ✅ **Thundering Herd** — 배치 발급 시 N명이 동시에 API 호출 → 순차 발급 또는 배치 크기 제한으로 해결
- ✅ **배치 크기 N 산정** — `N = 커넥션 수 × (주기 / 평균 처리 시간)`

#### 토큰 & TTL
- ✅ **입장 토큰 & TTL** — 토큰 없으면 주문 API 차단. TTL 너무 짧으면 주문 중 만료, 너무 길면 자리 점유
- ✅ **TTL 기준 산정** — P95 주문 완료 시간 + 여유 20~30%. 짧으면 결제 중 만료, 길면 자리 점유
- ✅ **토큰 검증 위치** — Filter(Spring 밖, Bean 불편), Interceptor(Spring 안, Bean+URL패턴 가능), AOP(메서드 레벨). 대기열 토큰은 Interceptor

#### 실시간 순번 조회
- ✅ **Polling vs SSE** — Polling: 연결 짧고 요청 많음 / SSE: 요청 적고 연결 10만 개 유지. 실무는 Polling
- ✅ **Polling 부하 완화** — 용량 기준으로 주기 설정 + Redis만 조회. 순번에 따라 주기 다르게 (Adaptive)
- ✅ **예상 대기 시간 계산 공식** — `내 순번 / N × 주기`. 오차 있음 (이탈자, 가변 처리시간). 정확한 값 아닌 안내용

#### 시스템 안정성
- ✅ **HikariCP 커넥션 풀 고갈** — 스레드 점유 → 메모리 OOM → 서버 다운. 대기열이 커넥션 수를 기준으로 N 제한
- ✅ **Redis 장애 시나리오** — Master-Replica로 확률 감소. 전부 죽으면 503(안전) vs 직접 허용(비즈니스). 트레이드오프로 결정

---

## 🗓️ 이번 주 (2026-03-30 ~ 2026-04-03)

### 마감
- **코드 PR**: 2026-04-03 (금) 18:00
- **블로그 (Technical Writing)**: 2026-04-03 (금) 18:00
- **WIL (Weekly I Learned)**: 2026-04-03 (금) 18:00

### 📅 일별 계획 (목요일까지 구현 완료)

| 날짜 | 목표 | 세부 내용 |
|------|------|----------|
| **화 (오늘)** | 개념 마무리 + 시뮬레이션 | 미학습 개념 중 설계에 필요한 것 → 시뮬레이션 Phase 1/2 실행 |
| **수** | 설계 확정 + 구현 시작 | 고민 목록 → 결정 사항 확정 → Step 1 TDD 시작 |
| **목** | 구현 완료 | Step 2, Step 3 TDD → Phase 3 검증 (Before/After) → PR 초안 |
| **금 오전** | PR + 블로그 + WIL | PR 다듬기 → 블로그 작성 → WIL 작성 → 18:00 제출 |

### ⚠️ 리스크 관리
- 수요일까지 설계 결정 못 하면 → 목요일 구현 시간 부족
- 시뮬레이션은 결과 수치만 기록하고 길게 보지 말 것 (최대 1~2시간)
- 블로그/WIL은 구현하면서 메모해둔 것 기반으로 작성 → 금요일 당일 처음 쓰지 말 것

### 주제
**Redis 기반 대기열 시스템** (Black Friday 주문 API 앞단)

---

## 🧪 Phase 0 — 시뮬레이션 (문제 재현 먼저)
<!-- 왜 이렇게 만들어야 하는지, 안 하면 뭐가 문제인지 직접 겪고 나서 구현 -->

### 시뮬레이션 1 — 대기열 없이 주문 API에 동시 요청 폭탄
> 목적: Rate Limiting도 없고 대기열도 없으면 어떻게 터지는지 확인

**재현 방법**
```bash
# k6로 200 VU가 동시에 주문 API 직접 호출
k6 run --vus 200 --duration 10s simulate/no-queue.js
```

**확인 포인트**
- [ ] DB 커넥션 풀(HikariCP) 고갈 → `Connection is not available` 에러
- [ ] 응답 시간 급격히 증가 (정상 20ms → 수 초)
- [ ] 재고 음수 or 중복 처리 발생 여부

**예상 결론**: → 대기열이 왜 필요한지 체감

---

### 시뮬레이션 2 — Sorted Set 없이 List로 대기열 구현
> 목적: 순서 보장과 중복 방지가 왜 Sorted Set이어야 하는지 확인

**재현 방법**
- `LPUSH queue:waiting userId` 로 단순 List 구현
- 동시에 같은 userId로 여러 번 진입 시도

**확인 포인트**
- [ ] 동일 userId가 List에 중복 등록되는지
- [ ] 순서가 뒤집히는 케이스 발생하는지 (LPUSH vs RPUSH)
- [ ] 순번 조회 시 O(N) 스캔 성능 문제

**예상 결론**: → Sorted Set의 `score 기반 순서 보장 + 중복 방지(ZADD NX)`가 왜 맞는지

---

### 시뮬레이션 3 — 토큰 없이 주문 API 바로 열어두기
> 목적: 대기열 통과 검증 없으면 어떤 일이 생기는지 확인

**재현 방법**
- 토큰 검증 인터셉터 비활성화 상태에서
- 대기열 진입 안 한 유저가 주문 API 직접 호출

**확인 포인트**
- [ ] 대기열 우회 가능한지
- [ ] 대기 중인 유저들이 손해 보는지

**예상 결론**: → 토큰 기반 입장 제어가 왜 필요한지

---

### 시뮬레이션 4 — TTL 없는 토큰
> 목적: 토큰에 만료 시간이 없으면 어떤 문제가 생기는지 확인

**재현 방법**
- Redis에 TTL 없이 토큰 저장
- 오래된 토큰으로 주문 API 재시도

**확인 포인트**
- [ ] 수 시간 후에도 토큰으로 주문 가능한지
- [ ] 대기열 공정성 훼손 여부 (대기 안 하고 나중에 사용)

**예상 결론**: → TTL 설계 기준의 의미

---

### 시뮬레이션 5 — 스케줄러 배치 크기를 너무 크게 잡았을 때
> 목적: 배치 크기 N이 DB 커넥션 풀보다 크면 어떻게 되는지 확인

**재현 방법**
- HikariCP `maximum-pool-size=10` 설정
- 스케줄러 배치 크기 N=50으로 설정 후 실행

**확인 포인트**
- [ ] 커넥션 대기 타임아웃 발생 여부
- [ ] 다른 API 응답 지연 여부

**예상 결론**: → 배치 크기 산정 근거 (`N ≤ 커넥션 풀 크기 × 주기 / 처리시간`)

---

## ✅ Checklist

### 🚪 Step 1 — 대기열
- [x] `POST /queue/enter` — Redis Sorted Set 기반 대기열 진입 API
- [x] `GET /queue/position` — 순번 조회 API
- [x] userId 기반 중복 진입 방지 (ZADD NX)
- [x] 전체 대기 인원 조회 (ZCARD)

### 🎫 Step 2 — 입장 토큰 & 스케줄러
- [x] 스케줄러: 주기적으로 대기열에서 N명 꺼내 입장 토큰 발급 (Lua 스크립트 + Redisson 락)
- [x] 토큰 TTL 설정 (5분)
- [x] 주문 API 진입 시 토큰 검증 (Interceptor)
- [x] 주문 완료 후 토큰 삭제 (TokenService.revoke)
- [x] 처리량 기준 스케줄러 배치 크기 산정 근거 문서화 (N=80, 시뮬레이션 후 조정)

### 📡 Step 3 — 실시간 순번 조회
- [x] 예상 대기 시간 계산 로직 (순번 / N × 주기)
- [x] Polling 기반 순번 + 예상 대기 시간 응답 (Adaptive nextPollAfter)
- [x] 토큰 발급 시 순번 조회 응답에 토큰 포함

### 🧪 검증
- [x] 동시 진입 테스트 — 같은 userId 동시 10회 → 1건만 등록, N명 동시 → 각자 고유 순번
- [x] 토큰 만료 테스트 — TTL 1초 강제 설정 후 2초 대기 → 토큰 무효화 확인
- [x] 처리량 초과 테스트 — 100명 진입, N=80 → 20명 잔여 / 2회 실행 → 전원 처리

---

## 🤔 설계 고민 목록 (미결)
<!-- 구현 전/중에 판단해야 할 것들. 결론 나면 아래 "설계 결정 사항"으로 이동 -->

### Step 1 — 대기열
- [x] **Redis Sorted Set score 기준**: `System.currentTimeMillis()` ✅
  - 고민: `currentTimeMillis()` 밀리초 충돌 가능성 vs `AtomicLong` 시퀀스 순서 완벽 보장
  - 선택: `System.currentTimeMillis()`
  - 이유: 밀리초 충돌은 극히 드물고, 같은 밀리초 내 순서는 비즈니스상 무의미. AtomicLong은 서버 재시작 시 초기화 문제 + 멀티 인스턴스 시 충돌 리스크
- [x] **중복 진입 방지 구현 방식**: `ZADD NX` ✅
  - 고민: ZSCORE 조회 후 조건 분기(2단계) 참조-TOCTOU 개념 파일(03-toctou.md) vs ZADD NX(1단계 원자적 처리)
  - 선택: ZADD NX (`addIfAbsent`)
  - 이유: ZSCORE 후 분기는 두 명령어 사이에 다른 스레드가 끼어드는 TOCTOU 문제 발생 가능. ZADD NX는 Redis 싱글 스레드 특성상 확인+추가가 원자적으로 처리되어 중복 불가능
- [x] **대기열 만료 처리**: Polling 기반 이탈 감지 후 자동 제거 ✅
  - 고민: 무기한 대기 vs Polling 없으면 이탈로 간주 후 제거
  - 선택: Polling 기반 이탈 감지 (마지막 Polling 시각 추적 → 일정 시간 초과 시 ZREM)
  - 이유: 무기한 대기는 유령 유저가 쌓여 실제 대기자 순번 왜곡
  - ⏳ **Polling 주기 + 이탈 판단 기준**: 시뮬레이션 후 결정

### Step 2 — 입장 토큰 & 스케줄러
- [x] **토큰 저장소**: Redis String ✅
  - 고민: Redis String (TTL 자동 만료, 빠른 조회) vs DB (장애 내성, TTL 직접 관리)
  - 선택: Redis String
  - 이유: 토큰 조회는 모든 주문 API 호출마다 발생 → DB 커넥션 소모 부담. Redis TTL로 만료 자동 처리
  - ⏳ **Redis vs DB 성능 비교 테스트**: 시뮬레이션에서 직접 체험 후 수치 기록
- [x] **스케줄러 배치 크기 N 산정**: 초기값 N=80 ✅
  - 고민: 실측 전이므로 가정값으로 시작, 시뮬레이션 후 조정
  - 선택: N=80 (커넥션 10 × (주기 5초 / 처리 500ms) × 버퍼 80%)
  - 이유: HikariCP 기본값 10, 처리시간 500ms 가정. 100% 사용 시 여유 없으므로 80%로 보수적 설정
  - ⏳ **주기 + 처리시간 실측 후 N 재산정**: 시뮬레이션 후 조정
- [x] **토큰 검증 위치**: Interceptor ✅
  - 고민: Filter(Spring 밖) vs Interceptor(Spring 안) vs AOP(메서드 레벨)
  - 선택: Interceptor
  - 이유: Redis 조회(Spring Bean 필요) + `/order/**` URL 패턴 적용 + CoreException → ControllerAdvice 처리
- [x] **스케줄러 중복 실행 방지**: Redisson 분산 락 구현 ✅
  - 고민: 단일 인스턴스는 문제 없지만, 멀티 인스턴스 시 스케줄러 중복 실행 → 같은 유저에게 토큰 중복 발급
  - 선택: Redisson 분산 락 (락 획득한 인스턴스만 실행, 나머지 스킵)
  - 이유: 멀티 인스턴스 환경 기준으로 공부 및 구현. 테스트 코드로 중복 실행 방지 검증

### Step 2 추가 고민 (구현 전 발견)

- [ ] **스케줄러 원자성 문제**: ZRANGE → ZREM → SET 3단계가 분리되어 있음
  - 고민: 중간 단계 실패 시 대기열에서 제거됐지만 토큰 없는 상태 발생 가능
  - 선택: Lua 스크립트로 원자적 처리
  - 이유: 3단계를 Redis 단일 스크립트로 묶으면 중간 실패 불가. 디버깅은 어렵지만 테스트로 정상 동작 검증 가능
  - 엣지케이스:
    - ZRANGE 후 ZREM 전 같은 userId가 재진입 시도 → Lua로 묶으면 방지됨
    - 토큰 발급 실패 시 대기열에서만 제거되는 유령 상태 → Lua로 방지

- [ ] **토큰 값 설계**: 단순 "1" vs UUID
  - 고민: 단순 값은 userId만 알면 토큰 존재 여부 유추 가능 → 타인 userId로 주문 API 호출 가능
  - 선택: UUID (응답에 토큰 값 포함하여 클라이언트가 헤더에 실어 보냄)
  - 이유: userId를 알아도 토큰 값 모르면 차단. 실무에 가까운 구조
  - 트레이드오프: 클라이언트가 토큰 값 저장 필요, 검증 로직이 "존재 여부" → "존재 + 값 일치"로 복잡해짐
  - 엣지케이스:
    - UUID 토큰을 헤더에 안 실으면 FORBIDDEN
    - 만료된 UUID로 재시도 → FORBIDDEN

- [ ] **토큰 보유 중 대기열 재진입**: 허용 vs 차단
  - 고민: 토큰 받은 유저가 주문 안 하고 다시 대기열 진입 시도 → 토큰 + 대기열 동시 존재 이상 상태
  - 선택: ⏳ 테스트로 이상 상태 재현 후 결정
  - 엣지케이스:
    - 이상 상태에서 스케줄러 실행 시 토큰 중복 발급 여부
    - 기존 토큰 TTL 리셋 여부

- [ ] **스케줄러 실행 시간 > 주기**: 중첩 실행 가능성
  - 고민: 5초 주기인데 처리가 7초 걸리면 이전 실행이 끝나기 전에 다음 실행 시작
  - 선택: Redisson 분산 락으로 방지 (락 획득 실패 시 스킵)
  - 이유: 분산 락이 중복 실행 방지 역할도 겸함

- [ ] **스케줄러 엣지케이스 목록**
  - 대기열 비어있을 때 실행 → 예외 없이 정상 종료
  - 대기열 인원 < N → 있는 만큼만 발급 (N=80인데 3명이면 3명만)
  - 분산 락 획득 실패 → 스킵 (로그만 남기고 정상 처리)

- [ ] **토큰 검증 엣지케이스 목록**
  - 토큰 없음 → FORBIDDEN
  - 만료된 토큰 → FORBIDDEN (Redis에서 TTL 만료 시 키 자동 삭제)
  - 빈 문자열 토큰 → FORBIDDEN
  - 다른 userId의 토큰 값으로 시도 → FORBIDDEN (UUID 불일치)

### Step 3 — 실시간 순번 조회
- [x] **예상 대기 시간 계산 공식**: `내 순번 / N × 주기` ✅
  - 고민: 기본 공식 vs 이탈자/가변 처리시간 반영한 정교한 공식
  - 선택: `내 순번 / N × 주기`
  - 이유: 정교한 공식은 복잡도 대비 정확도 개선 미미. 어차피 안내용이므로 단순 공식으로 충분
- [x] **Polling 주기**: Adaptive Polling 구현 ✅
  - 고민: 고정 주기 vs 서버가 nextPollAfter 포함한 Adaptive
  - 선택: Adaptive (응답에 `nextPollAfter` 포함, 순번에 따라 주기 다르게)
  - ⏳ **구체적 주기 수치**: 시뮬레이션 후 결정
- [x] **Polling 부하 완화**: Redis only (ZRANK) ✅
  - 고민: DB 조회 포함 vs Redis ZRANK만 조회
  - 선택: Redis only (`ZRANK`, `ZCARD`)
  - 이유: 순번 조회는 모든 대기자가 주기적으로 호출 → DB 커넥션을 주문 API와 경쟁하지 않도록
  - 트레이드오프:
    - Redis 장애 시 순번 조회 불가 (단, 주문 API도 이미 불가 → 허용 가능한 수준)
    - Redis 데이터가 유일한 진실의 원천 → DB와 동기화 없음 (설계상 의도)
  - 엣지케이스:
    - 토큰 발급 직후 ZREM 전에 Polling 오면 순번이 아직 남아있을 수 있음 → 다음 Polling에서 해소
    - ZRANK는 0-indexed → 클라이언트에 내려줄 때 +1 처리 필요
    - 대기열이 비어있을 때 ZRANK → null 반환 → null 체크 필수

---

## 💭 설계 결정 사항 (확정)
<!-- 고민 끝에 내린 판단들. 블로그/PR 소재가 됩니다 -->

| 항목 | 결정 | 이유 |
|------|------|------|
| Polling vs SSE | Polling + Adaptive | 연결 유지 비용 없음, 주기 조절로 부하 제어 |
| 토큰 저장소 | Redis String | TTL 자동 만료, 빠른 조회 |
| 토큰 검증 위치 | Interceptor | Spring Bean + URL 패턴 + ControllerAdvice |
| 스케줄러 배치 크기 N | 초기값 80, 시뮬레이션 후 조정 | 커넥션 10 × 주기5s / 처리500ms × 80% |
| 스케줄러 중복 실행 방지 | Redisson 분산 락 | 멀티 인스턴스 환경 기준 |
| score 기준 | currentTimeMillis() | 밀리초 충돌 비즈니스상 무의미 |
| 중복 진입 방지 | ZADD NX | 원자적 처리, TOCTOU 방지 |
| 대기열 만료 처리 | Polling 기반 이탈 감지 | 유령 유저 순번 왜곡 방지 |
| 예상 대기 시간 공식 | 순번 / N × 주기 | 안내용, 단순 공식으로 충분 |
| Polling 부하 완화 | Redis only (ZRANK) | DB 커넥션 경쟁 없음 |
| Redis 장애 시 fallback | 503 반환 | 안전 우선, 팀 결정에 따라 변경 가능 |

---

## 📝 진행 로그
<!-- 매일 작업 후 간략히 기록 -->

### 2026-03-30 (월)
- 프로젝트 세팅 및 컨텍스트 파일 생성
- 작업 시작 예정

---

## 🔖 다음 세션에 이어서 할 일
<!-- 세션 종료 전 반드시 업데이트 -->
- Step 1 설계부터 시작: Redis Sorted Set 구조 결정, 패키지 구조 설계

---

## ✍️ 블로그 & WIL 소재 메모
<!-- 구현하다 떠오르는 인사이트, 고민, 트레이드오프 메모 -->

### 블로그 후보 주제 (Technical Writing)
- Rate Limiting으로 거부하는 것 vs 대기열로 줄 세우는 것: 어떤 상황에서?
- 스케줄러 배치 크기 산정 근거 (DB 커넥션 풀, 평균 처리 시간 기준)
- Thundering Herd 대응
- Redis 장애 시 서비스는 어떻게 되어야 하는가?
- Polling vs SSE — 왜 그 방식을 선택했는가?
- 토큰 TTL을 몇 분으로 설정했고, 그 기준은?

### WIL (Weekly I Learned) 작성 가이드
> WIL은 블로그보다 가볍게, "이번 주 내가 배운 것"을 솔직하게 정리하는 글

**작성 항목**
- 이번 주 배운 기술/개념 (Redis Sorted Set, 토큰 TTL 설계, Polling 부하 등)
- 가장 어려웠던 부분과 해결 과정
- 잘 됐던 것 vs 아쉬웠던 것
- 다음 주에 가져갈 인사이트

---

## 📋 PR 작성 양식 (참고 템플릿)
<!-- PR 작성 시 이 구조를 따를 것 -->

```
📌 Summary
한 단락으로 이번 PR의 핵심을 요약 (무엇을, 왜, 핵심 결과)
불릿으로 핵심 포인트 3~5개

🧭 Context & Decision
문제 정의 (코드 블록으로 Before 구조 표현)
리스크 테이블
핵심 결정 요약 테이블 (#, 결정 항목, 최종 선택, 핵심 근거)

각 결정별:
  - 의도 (왜 이 결정을 내렸는가)
  - 코드 스니펫 (Before/After 비교)
  - 설계도 (Mermaid 다이어그램)  ← 중간중간 흐름도 필수!
  - 대안 비교 테이블 (전략 / 장단점 / 선택 이유)
  - 관련 클래스 테이블 (컴포넌트 / 파일경로 / 메서드 / 역할)

🏗️ Design Overview
변경 범위 (기존 변경 / 신규 추가)
주요 컴포넌트 & 핵심 메서드 테이블

🔁 Flow Diagram
Mermaid로 전체 흐름 다이어그램  ← 설계도 필수!

✅ Checklist
Step별 체크리스트

🔍 리뷰 포인트
멘토/리뷰어에게 묻고 싶은 판단 2~3개
  - 해결하려는 비즈니스 문제
  - 시도한 접근
  - 코드 위치
  - 이렇게 선택한 이유
  - 인지하고 있는 트레이드오프
  - 여쭙고 싶은 점
```

### PR 작성 시 핵심 원칙
- "무엇을 했다" 가 아니라 **"왜 그렇게 판단했는가"** 중심
- 각 결정마다 **대안 비교 테이블** 포함 (왜 이걸 선택했고 나머지는 왜 버렸는지)
- **Mermaid 설계도** 중간중간 삽입 (흐름도, 상태 머신, 시퀀스 다이어그램)
- 리뷰 포인트에서 **솔직하게 판단이 서지 않는 부분** 질문

---

## 🏗️ 프로젝트 구조 (참고)
```
apps/commerce-api/src/main/java/com/loopers/
├── application/    # Facade (usecase 조합)
├── domain/         # Entity, Service, Repository interface
├── infrastructure/ # Repository 구현체 (JPA, Redis)
├── interfaces/api/ # Controller, DTO, ApiSpec
└── support/        # CoreException, ErrorType
```

**Tech Stack**: Java 21, Spring Boot 3.4.4, Redis, MySQL 8.0, Kafka
